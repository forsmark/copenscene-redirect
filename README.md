# copenscene-redirect

Forwards `copenscene.rocks` to where CopenScene is served, keeping the path.
GitHub Pages holds the certificate for the domain. See ADR 0013 in
`forsmark/copenscene`.

To move the site, change the address in `index.html` and `404.html`.
